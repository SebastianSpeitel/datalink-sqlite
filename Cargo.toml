[package]
name = "datalink_sqlite"
version = "0.1.2"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
default = ["migrations"]
migrations = []


[dependencies]
datalink = { git = "https://github.com/SebastianSpeitel/datalink", version = "0.3", rev = "13f6b07", features = [
    "random",
    "unique",
] }
log = "*"
rusqlite = { version = "0.31", features = [] }
thiserror = "1.0"

[dev-dependencies]
env_logger = "*"
